name: proposing
description: ProPosing - Sistema de Análise de Poses de Fisiculturismo
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  camera: ^0.10.5
  camera_macos: ^0.0.9
  provider: ^6.1.0

# Usa versão patcheada local para eliminar warnings de APIs deprecadas (AVCaptureDevice)
dependency_overrides:
  camera_macos:
    path: packages/camera_macos

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

